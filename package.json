{
  "name": "ts-gcp-firestore",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "tslint -c tslint.json -p tsconfig.json --fix",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "npm run lint && tsc"
  },
  "author": "dhryn-public",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/firestore": "^2.6.0"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:dhryn-public/ts-gcp-firestore.git"
  },
  "devDependencies": {
    "@types/node": "^12.12.6",
    "tslint": "^5.20.1",
    "typescript": "^3.7.2"
  }
}
